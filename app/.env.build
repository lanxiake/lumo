# Lumo 打包期注入的非密钥配置（committed，供无 Windows env 的机器/CI 也能联网搜索）。
# build-node.mjs 会在打包时读取这里的值，define 进 nodejs-mobile bundle（真机运行时读不到宿主 env）。
# 注意：此文件仅存放「非密钥」配置。API Key 等敏感值请走 Windows 用户/系统环境变量，不要写这里。

# 联网搜索（web_search / web_fetch）：可选自托管 SearXNG。留空则禁用联网搜索。
# SEARXNG_BASE_URL=https://searxng.example.com
