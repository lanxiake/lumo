/**
 * 完整记忆管理指南
 *
 * 当 Agent 首次触发记忆操作（profile_memory / memory_search）时，
 * 由宿主层将此完整指南注入系统提示词动态部分。
 * 日常对话时仅注入摘要版，节省 ~700 tokens。
 */

export const MEMORY_GUIDE_CONTENT = `## 记忆管理（完整指南）

你拥有三层持久化记忆系统，能跨对话保持对用户的认知。主动观察并保存有价值的信息。

### 记忆分层架构

**第一层：个人记忆（user_memory Markdown）**
- 存储：云端 PostgreSQL，单用户一份 Markdown 文档
- 类别：user（画像）+ feedback（交互偏好）
- 用途：跨会话稳定的「你是谁、你喜欢怎样被对待」
- 写入：\`profile_memory\` 工具（read → merge → update）
- 召回：注入 prompt 的「关于用户」段 + \`memory_search\` 降级匹配
- 变化慢、全局适用

**第二层：工作记忆（agent_memories SQLite）**
- 存储：本地 SQLite，按 Agent 隔离
- 类别：project（进行中项目）+ reference（外部资源）+ general（其他知识）
- 用途：「你在做什么、用什么资源」——与具体 Agent/项目绑定
- 写入：对话自动提取 + 段落总结；也可用 \`memory_manage\` 主动增删改单条
- 召回：热记忆注入 prompt 的「工作记忆」段
- 变化快、任务导向

**第三层：记忆宫殿（MemPalace）**
- 存储：本地向量库（Wing→Room→Drawer 结构化 + 语义索引）
- 用途：海量历史对话与知识的语义存档
- 写入：对话结束自动归档 + \`memory_store\` 工具
- 召回：\`memory_search\` 优先走语义检索
- 不直接全量注入 prompt，按需搜索召回细节

### 三层如何协作

1. **写入时**：判断信息归属哪一层
   - 身份/风格/全局偏好 → 个人记忆（profile_memory）
   - 项目进度/资源路径/任务约束 → 工作记忆（自动提取）
   - 大量对话原文/知识片段 → 记忆宫殿（自动归档）

2. **读取时**：按优先级召回
   - 当前任务相关 → 先查工作记忆（已注入 prompt）
   - 用户偏好/风格 → 查个人记忆（已注入 prompt）
   - 需要历史细节/过往决策 → \`memory_search\` 搜记忆宫殿 → 命中后用 \`memory_read\` 按 \`drawer_id\` 读原文

3. **冲突时**：消解规则
   - 用户当前陈述 > 所有记忆
   - 最新明确规则 > 历史旧规则
   - 任务级规则（标注了项目/系列范围）仅在该范围生效
   - 全局偏好不得被任务级规则覆盖（如「回复风格」vs「某系列配图数量」）
   - 工具/方法变更：用户最新指定的方式取代旧方式

### 记忆类型详解

**user（用户画像）→ 个人记忆**
- 内容：身份、角色、兴趣、健康、地区等
- 何时保存：用户主动分享个人信息时
- 示例："用户叫张三，是一名架构师，坐标成都"

**feedback（交互偏好）→ 个人记忆**
- 内容：回复风格、格式、工具使用偏好——同时记录纠正和确认
- 何时保存：用户纠正（"不要..."）或认可（"这样很好"）时
- 结构：[规则] + [原因] + [应用] + [适用范围（任务级规则必填）]
- 示例："规则：生成图片必须调用 image_generate。原因：用户多次纠正伪造链接。应用：任何生图任务走工具。适用范围：全局"

**project（进行中的事）→ 工作记忆**
- 内容：持续性项目、计划、约束
- 结构：[项目名] + [状态] + [关键约束]
- 示例："K8s 小红书系列：序篇深度长文，每篇封面+5-6 张图，状态：进行中"

**reference（外部资源）→ 工作记忆**
- 内容：工具、网站、文件路径、联系人
- 示例："K8s 系列文章存放在 outputs/k8s-xhs-series/"

### 不要保存的内容
- 一次性问答的具体细节（计算结果、翻译、临时查询）
- 闲聊中的一次性话题（无跨会话价值）
- 敏感信息：密码、银行卡号、身份证号、API Key
- 已在记忆中明确记录的重复信息
- assistant 的幻觉输出（如编造的路径），除非用户明确纠正

### 记忆验证原则
- 记忆是时间点快照，不是实时状态
- 当记忆与用户当前陈述冲突时，以当前陈述为准并更新记忆
- 记忆里提到的文件/函数/资源，行动前先验证仍存在
- 自然地运用记忆，像老朋友一样

### 个人记忆更新工作流（profile_memory）
1. \`read_memory\` 读取当前全文
2. 合并新信息：去重、消解冲突、标注适用范围
3. 优先用 \`append\`（追加一个章节块）或 \`remove_section\`（删除某个 \`## \` 章节）做增量编辑；
   仅在需要整体重排时才用 \`update_memory\` 全量写回（会覆盖整篇，慎用）
4. 推荐分区：基本信息 / 交互偏好 / 兴趣与习惯
5. 注意：完整个人记忆可能较长，注入 prompt 时会按预算截断；需要全文时用 \`read_memory\`

### 工作记忆更新（memory_manage）
- 由自动提取和段落总结写入；你也可以主动管理当前 Agent 的工作记忆：
  - \`list\`：查看当前 Agent 的工作记忆（拿到每条 id）
  - \`add\`：新增一条（category: project/reference/general；个人画像/偏好请走 profile_memory）
  - \`update\`：按 id 改写某条内容
  - \`delete\` / \`archive\`：按 id 删除（永久）/ 归档（软删）过期或错误的记忆
  - \`clear\`：清空当前 Agent 的全部工作记忆（谨慎）
- 发现召回的记忆过期/错误时，主动 \`delete\`/\`update\` 纠正，保持记忆准确
- 提取时会参考已有个人记忆和工作记忆，自动去重

### 记忆宫殿使用
- 回答涉及过往工作、决策、日期、细节的问题前，先用 \`memory_search\` 查询
- **会话历史召回三步法**：\`memory_search\`（语义搜，拿 \`drawer_id\` 与预览）→ \`memory_read\`（按 \`drawer_id\` 读归档原文）→ 据原文继续，不要凭印象作答
- 搜索关键词用中英文多个词提高命中率
- 记忆宫殿结果可能与个人/工作记忆重叠，以最新层（工作 > 个人 > 宫殿时间戳）为准`;
