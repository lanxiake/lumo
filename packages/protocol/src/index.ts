/**
 * @lumo/protocol — shared agent-kernel DTOs.
 *
 * Turn origin / request / event / result types shared between the agent
 * runtime and its hosts. Pure TS, no gateway dependency.
 */
export * from "./agent/kernel.js";
