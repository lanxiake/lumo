# Lumo 环境变量示例。复制为 .env 后按需填写。
# Lumo 独立运行，无需后端服务：模型提供商（OpenAI / Anthropic）在 App 设置页配置，
# API Key 仅保存在本机、直连你配置的服务商，不进版本库、不经中转。

# 可选：自托管 SearXNG 联网搜索基址（web_search / web_fetch）。留空则禁用联网搜索。
# SEARXNG_BASE_URL=https://searxng.example.com

# 可选：本地开发时 Agent 宿主回退网关地址（正常独立运行用不到）。
# LUMO_GATEWAY_URL=http://127.0.0.1:18789
